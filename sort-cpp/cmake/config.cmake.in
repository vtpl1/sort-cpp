@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_package(OpenCV REQUIRED)

if(NOT TARGET sort_cpp::sort_cpp)
    include(${CMAKE_CURRENT_LIST_DIR}/sort_cpp-targets.cmake)
endif()

check_required_components(sort_cpp)